#include <stdio.h>
#include "shared_func.h"

void daemon_init(bool bCloseFile)
{
	printf("daemon init done!\n");

	return;
}

int init_pthread_lock(pthread_mutex_t *pthread_lock)
{
	printf("init_pthread_lock done!\n");

	return 0;
}