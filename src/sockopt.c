#include "sockopt.h"
#include <stdio.h>

int socketServer(const char *bind_addr,const int port,const char *LogFilePrex)
{
	printf("socketSercer done!\n");

	return 0;
}

int nbaccept(int sock,int timeout,int *err_no)
{
	printf("nbaccept done!\n");

	return 0;
}